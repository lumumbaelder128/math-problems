#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
    // Initialize variables
    int num1 = 5, num2 = 3;

    // Calculate the sum of the two numbers
    int sum = num1 + num2;

    // Print the result
    cout << "The sum is: " << sum << endl;

    return 0;
}
